rmdir(1)--删除空目录
=========================

##总览

`rmdir [-p] dir...`

##描述

rmdir删除空目录。如果所给出的目录不为空，则报错。

##选项

* `-p,--parents`: 
如果目录由多个路径组成，从最后一个路径开始依次删除，直到所有的路
径都被删完。命令"`rmdir -p a/b/c`"按照"`rmdir /a/b/c; rmdir a/b;
rmdir a`"的顺序删除目录

* `--ignore-fail-on-non-empty`:
通常，rmdir拒绝删除非空目录。这个选项将使命令忽略非空目录而进行
删除操作。

* `--help`:
在标准输出上显示使用信息并顺利退出

* `--version`:
在标准输出上显示版本信息并顺利退出
	
##EXAMPLES

命令`rmdir foo`将删除空目录foo。要删除一个非空目录，并且包括其下属所有内
容，可以使用命令'rm -r foo'。
